<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://arim0317-dot.github.io/my-blog/feed.xml" rel="self" type="application/atom+xml" /><link href="https://arim0317-dot.github.io/my-blog/" rel="alternate" type="text/html" /><updated>2026-09-01T17:44:02+09:00</updated><id>https://arim0317-dot.github.io/my-blog/feed.xml</id><title type="html">아림의 블로그</title><subtitle>개발을 배우며 그날그날 정리한 것을 기록하는 아림의 학습 블로그입니다.</subtitle><author><name>신아림</name></author><entry><title type="html">GitHub 원격 저장소 연결하기</title><link href="https://arim0317-dot.github.io/my-blog/github-remote/" rel="alternate" type="text/html" title="GitHub 원격 저장소 연결하기" /><published>2026-08-31T00:00:00+09:00</published><updated>2026-08-31T00:00:00+09:00</updated><id>https://arim0317-dot.github.io/my-blog/github-remote</id><content type="html" xml:base="https://arim0317-dot.github.io/my-blog/github-remote/"><![CDATA[<p>지금까지는 <code class="language-plaintext highlighter-rouge">git push</code>, <code class="language-plaintext highlighter-rouge">git pull</code>을 그냥 명령어로만 써봤는데,
오늘은 <strong>내 컴퓨터의 Local Repository가 어떻게 GitHub 저장소랑 연결되는지</strong>를 정리해본다.</p>

<h2 id="1-왜-연결이-필요할까">1. 왜 “연결”이 필요할까</h2>

<p><code class="language-plaintext highlighter-rouge">git push</code>를 치면 Git은 “어디로 올려야 하는지” 알아야 한다.
그 “어디”를 미리 알려주는 작업이 바로 <strong>원격 저장소 연결</strong>이다.</p>

<pre><code class="language-mermaid">flowchart LR
    A[내 컴퓨터&lt;br&gt;Local Repository] -- 연결 안 되어 있으면 --&gt; X["❌ push 불가"]
    A -- git remote add origin --&gt; B[GitHub 저장소]
    B -- git push --&gt; C["✅ 업로드 완료"]
</code></pre>

<h2 id="2-연결하는-두-가지-상황">2. 연결하는 두 가지 상황</h2>

<p>시작하는 방향에 따라 방법이 다르다.</p>

<h3 id="상황-1-이미-내-컴퓨터에-저장소가-있는-경우">상황 1) 이미 내 컴퓨터에 저장소가 있는 경우</h3>

<p>로컬에서 <code class="language-plaintext highlighter-rouge">git init</code>으로 저장소를 만들고 커밋도 해놨는데,
GitHub에는 아직 빈 저장소만 있는 경우다. 이때는 <code class="language-plaintext highlighter-rouge">git remote add</code>로 둘을 연결한다.</p>

<pre><code class="language-mermaid">flowchart LR
    A[git init 으로 만든&lt;br&gt;Local Repository] -- git remote add origin --&gt; B[GitHub의 빈 저장소]
</code></pre>

<h3 id="상황-2-github에-있는-저장소를-처음-받아오는-경우">상황 2) GitHub에 있는 저장소를 처음 받아오는 경우</h3>

<p>다른 사람이 만든 저장소, 혹은 예전에 GitHub에만 올려둔 저장소를 내 컴퓨터로 가져오고 싶을 때는
<code class="language-plaintext highlighter-rouge">git clone</code>을 쓴다. clone은 “복사해서 가져오면서 자동으로 연결까지 해준다”는 명령이다.</p>

<pre><code class="language-mermaid">flowchart LR
    A[GitHub 저장소] -- git clone --&gt; B[내 컴퓨터에&lt;br&gt;새로 생긴 Local Repository]
</code></pre>

<h2 id="3-명령어-정리">3. 명령어 정리</h2>

<table>
  <thead>
    <tr>
      <th>명령어</th>
      <th>언제 쓰나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git remote add origin &lt;주소&gt;</code></td>
      <td>로컬 저장소를 GitHub 저장소와 처음 연결할 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git remote -v</code></td>
      <td>지금 어떤 원격 저장소와 연결돼 있는지 확인하고 싶을 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git clone &lt;주소&gt;</code></td>
      <td>GitHub에 있는 저장소를 내 컴퓨터로 그대로 가져오고 싶을 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git push -u origin main</code></td>
      <td>연결한 뒤 처음으로 push하면서, 앞으로는 <code class="language-plaintext highlighter-rouge">git push</code>만 쳐도 되게 만들고 싶을 때</td>
    </tr>
  </tbody>
</table>

<h2 id="4-origin이-뭘까">4. origin이 뭘까</h2>

<p><code class="language-plaintext highlighter-rouge">origin</code>은 원격 저장소 주소한테 붙이는 <strong>별명</strong>이다.
매번 긴 GitHub 주소를 직접 치는 대신, <code class="language-plaintext highlighter-rouge">origin</code>이라는 짧은 이름으로 부르는 것이다.</p>

<table>
  <thead>
    <tr>
      <th>실제로 하는 일</th>
      <th>우리 눈에 보이는 것</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">https://github.com/아이디/저장소.git</code> 라는 주소</td>
      <td><code class="language-plaintext highlighter-rouge">origin</code> 이라는 이름</td>
    </tr>
  </tbody>
</table>

<h2 id="5--u는-왜-붙일까">5. -u는 왜 붙일까</h2>

<p>처음 push할 때 <code class="language-plaintext highlighter-rouge">git push -u origin main</code>처럼 <code class="language-plaintext highlighter-rouge">-u</code>를 붙이면,
“내 로컬의 main 브랜치는 앞으로 origin의 main 브랜치랑 짝이다”라고 기억시켜주는 것이다.
그래서 다음번부터는 <code class="language-plaintext highlighter-rouge">git push</code>, <code class="language-plaintext highlighter-rouge">git pull</code>만 쳐도 Git이 어디로 가야 할지 스스로 안다.</p>

<h2 id="오늘-정리하며-느낀-것">오늘 정리하며 느낀 것</h2>

<p>지금까지 push/pull은 그냥 “올리고 받아오는 명령”으로만 알고 있었는데,
사실 그 전에 <strong>로컬 저장소와 GitHub 저장소를 연결하는 과정</strong>이 먼저 있었다는 걸 알게 됐다.
연결만 한 번 잘 해두면, 그 다음부터는 지금까지 배운 push/pull/merge를 그대로 쓰면 된다.</p>]]></content><author><name>신아림</name></author><summary type="html"><![CDATA[지금까지는 git push, git pull을 그냥 명령어로만 써봤는데, 오늘은 내 컴퓨터의 Local Repository가 어떻게 GitHub 저장소랑 연결되는지를 정리해본다.]]></summary></entry><entry><title type="html">이번 주 배운 Git 명령어 정리</title><link href="https://arim0317-dot.github.io/my-blog/git-workflow/" rel="alternate" type="text/html" title="이번 주 배운 Git 명령어 정리" /><published>2026-08-31T00:00:00+09:00</published><updated>2026-08-31T00:00:00+09:00</updated><id>https://arim0317-dot.github.io/my-blog/git-workflow</id><content type="html" xml:base="https://arim0317-dot.github.io/my-blog/git-workflow/"><![CDATA[<p>이번 주에는 <strong>저장소 만들기 → 스테이징 → 커밋 → 브랜치 → push/pull → merge → 되돌리기</strong>까지,
Git으로 코드를 관리하는 전체 흐름을 배웠다. 오늘은 이걸 한 번에 정리해본다.</p>

<h2 id="1-전체-흐름-한눈에-보기">1. 전체 흐름 한눈에 보기</h2>

<p>Git 작업은 크게 네 개의 공간을 왔다 갔다 하는 일이다.</p>

<ul>
  <li><strong>작업 폴더</strong>: 내가 실제로 파일을 고치는 곳</li>
  <li><strong>스테이징 영역</strong>: 다음 커밋에 담을 파일을 골라두는 곳</li>
  <li><strong>Local Repository</strong>: 커밋(저장 기록)이 쌓이는, 내 컴퓨터 안의 저장소</li>
  <li><strong>원격 저장소(GitHub)</strong>: 인터넷에 올라가 있는 저장소</li>
</ul>

<pre><code class="language-mermaid">flowchart LR
    A[작업 폴더] -- git add --&gt; B[스테이징 영역]
    B -- git commit --&gt; C[Local Repository]
    C -- git push --&gt; D[원격 저장소 GitHub]
    D -- git pull --&gt; A
</code></pre>

<table>
  <thead>
    <tr>
      <th>명령어</th>
      <th>언제 쓰나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git init</code></td>
      <td>폴더를 Git 저장소로 처음 만들 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git add</code></td>
      <td>커밋에 담을 파일을 스테이징 영역에 올릴 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git commit</code></td>
      <td>스테이징된 내용을 Local Repository에 기록으로 남길 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git branch</code></td>
      <td>새로운 브랜치를 만들거나 목록을 볼 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git push</code></td>
      <td>Local Repository의 기록을 GitHub로 올릴 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git pull</code></td>
      <td>GitHub의 최신 기록을 내 컴퓨터로 받아올 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git merge</code></td>
      <td>다른 브랜치의 내용을 지금 브랜치로 합칠 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git reset</code></td>
      <td>스테이징하거나 커밋한 것을 되돌리고 싶을 때</td>
    </tr>
  </tbody>
</table>

<h2 id="2-저장소-만들기">2. 저장소 만들기</h2>

<p><code class="language-plaintext highlighter-rouge">git init</code>은 지금 폴더를 “Git이 관리하는 폴더”로 선언하는 명령이다.
이걸 해야만 add, commit 같은 다른 명령들이 의미를 가진다.</p>

<pre><code class="language-mermaid">flowchart LR
    A[그냥 평범한 폴더] -- git init --&gt; B[Git 저장소]
</code></pre>

<h2 id="3-스테이징">3. 스테이징</h2>

<p>파일을 두 개 고쳐도 그중 하나만 커밋하고 싶을 때가 있다.
그래서 Git은 “커밋하기 전에 담을 것부터 고르는” 단계를 따로 둔다. 이게 스테이징이다.</p>

<pre><code class="language-mermaid">flowchart LR
    A["파일 수정 (a.md, b.md)"] -- "git add a.md" --&gt; B["스테이징 영역 (a.md만 담김)"]
</code></pre>

<h2 id="4-local-repository와-커밋">4. Local Repository와 커밋</h2>

<p><code class="language-plaintext highlighter-rouge">git commit</code>을 하면 스테이징된 내용이 하나의 기록(스냅샷)으로 Local Repository에 저장된다.
Local Repository는 이런 기록들이 시간 순서대로 쌓여 있는, 내 컴퓨터 안의 저장소다.</p>

<table>
  <thead>
    <tr>
      <th>개념</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>스테이징 영역</td>
      <td>커밋 직전에 담을 파일을 잠깐 모아두는 곳</td>
    </tr>
    <tr>
      <td>Local Repository</td>
      <td>커밋(기록)들이 실제로 저장되는 곳</td>
    </tr>
    <tr>
      <td>커밋</td>
      <td>“지금 상태를 하나의 기록으로 남긴다”는 행동</td>
    </tr>
  </tbody>
</table>

<pre><code class="language-mermaid">flowchart LR
    A[스테이징 영역] -- git commit --&gt; B((커밋 1))
    B --&gt; C((커밋 2))
    C --&gt; D((커밋 3))
    subgraph Local Repository
    B
    C
    D
    end
</code></pre>

<h2 id="5-브랜치">5. 브랜치</h2>

<p>브랜치는 “지금 상태에서 갈라져 나온 또 다른 작업 줄기”다.
기존 코드를 건드리지 않고 새 기능을 실험해보고 싶을 때 브랜치를 만든다.</p>

<pre><code class="language-mermaid">gitGraph
    commit id: "커밋 1"
    commit id: "커밋 2"
    branch new-feature
    checkout new-feature
    commit id: "커밋 3"
</code></pre>

<h2 id="6-push와-pull">6. push와 pull</h2>

<p><code class="language-plaintext highlighter-rouge">push</code>는 내 Local Repository의 기록을 GitHub로 올리는 것이고,
<code class="language-plaintext highlighter-rouge">pull</code>은 반대로 GitHub의 최신 기록을 내 컴퓨터로 받아오는 것이다.</p>

<pre><code class="language-mermaid">flowchart TB
    subgraph 내 컴퓨터
    L[Local Repository]
    end
    subgraph 인터넷
    R[GitHub 원격 저장소]
    end
    L -- git push --&gt; R
    R -- git pull --&gt; L
</code></pre>

<h2 id="7-merge">7. merge</h2>

<p>merge는 브랜치에서 따로 진행한 작업을 원래 브랜치로 합치는 것이다.
예를 들어 new-feature 브랜치에서 만든 커밋을 main 브랜치로 가져오고 싶을 때 쓴다.</p>

<pre><code class="language-mermaid">gitGraph
    commit id: "커밋 1"
    commit id: "커밋 2"
    branch new-feature
    checkout new-feature
    commit id: "커밋 3"
    checkout main
    merge new-feature id: "merge"
</code></pre>

<h2 id="8-되돌리기">8. 되돌리기</h2>

<p>실수로 파일을 잘못 스테이징했거나, 아직 push하지 않은 커밋을 취소하고 싶을 때 <code class="language-plaintext highlighter-rouge">git reset</code>을 쓴다.
Local Repository 안에서만 벌어지는 일을 되돌리는 명령이라고 생각하면 된다.</p>

<pre><code class="language-mermaid">flowchart LR
    A[스테이징 또는 커밋한 상태] -- git reset --&gt; B[되돌린 상태]
</code></pre>

<h2 id="오늘-정리하며-느낀-것">오늘 정리하며 느낀 것</h2>

<p>이번 주 흐름을 한 문장으로 요약하면:</p>

<blockquote>
  <p>파일을 고치고(작업 폴더) → 담을 것을 고르고(스테이징) → 기록으로 남기고(커밋, Local Repository) →
필요하면 갈라져서 작업하고(브랜치) → 합치고(merge) → 인터넷에 올리고 받아오고(push, pull) →
잘못됐으면 되돌린다(되돌리기).</p>
</blockquote>

<p>이 순서만 기억하면 이번 주에 배운 명령어는 거의 다 설명이 된다.</p>]]></content><author><name>신아림</name></author><summary type="html"><![CDATA[이번 주에는 저장소 만들기 → 스테이징 → 커밋 → 브랜치 → push/pull → merge → 되돌리기까지, Git으로 코드를 관리하는 전체 흐름을 배웠다. 오늘은 이걸 한 번에 정리해본다.]]></summary></entry><entry><title type="html">지금까지 배운 것 정리 + 오늘 배운 것: Claude와 VSCode, 그리고 질문하는 법</title><link href="https://arim0317-dot.github.io/my-blog/claude-vscode-prompt/" rel="alternate" type="text/html" title="지금까지 배운 것 정리 + 오늘 배운 것: Claude와 VSCode, 그리고 질문하는 법" /><published>2026-08-31T00:00:00+09:00</published><updated>2026-08-31T00:00:00+09:00</updated><id>https://arim0317-dot.github.io/my-blog/claude-vscode-prompt</id><content type="html" xml:base="https://arim0317-dot.github.io/my-blog/claude-vscode-prompt/"><![CDATA[<p>오늘은 새로운 걸 두 가지 배웠다. <strong>Claude를 VSCode랑 같이 쓰는 법</strong>, 그리고
<strong>AI에게 물어볼 때 맥락/역할/제약/예시로 나눠서 써야 한다는 것</strong>이다.
전에 배운 Git 내용도 같이 정리해서, 지금까지 배운 걸 한 번에 모아본다.</p>

<h2 id="1-지금까지-배운-것-git">1. 지금까지 배운 것: Git</h2>

<p>Git은 코드를 저장하고 되돌릴 수 있게 해주는 도구다.</p>

<table>
  <thead>
    <tr>
      <th>명령어/개념</th>
      <th>언제 쓰나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git init</code></td>
      <td>폴더를 Git 저장소로 처음 만들 때</td>
    </tr>
    <tr>
      <td>스테이징 (<code class="language-plaintext highlighter-rouge">git add</code>)</td>
      <td>커밋에 담을 파일을 미리 고를 때</td>
    </tr>
    <tr>
      <td>커밋 (<code class="language-plaintext highlighter-rouge">git commit</code>)</td>
      <td>지금 상태를 하나의 기록으로 남길 때</td>
    </tr>
    <tr>
      <td>브랜치 (<code class="language-plaintext highlighter-rouge">git branch</code>)</td>
      <td>원래 코드를 안 건드리고 따로 작업하고 싶을 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git push</code></td>
      <td>내 컴퓨터의 기록을 GitHub로 올릴 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git pull</code></td>
      <td>GitHub의 최신 기록을 내 컴퓨터로 받아올 때</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">git merge</code></td>
      <td>다른 브랜치에서 한 작업을 합칠 때</td>
    </tr>
    <tr>
      <td>되돌리기 (<code class="language-plaintext highlighter-rouge">git reset</code>)</td>
      <td>잘못 스테이징하거나 커밋한 걸 취소하고 싶을 때</td>
    </tr>
  </tbody>
</table>

<h2 id="2-오늘-배운-것-claude를-vscode에서-쓰기">2. 오늘 배운 것: Claude를 VSCode에서 쓰기</h2>

<p>지금까지는 Claude를 따로 켜서 썼는데, 오늘은 <strong>VSCode 안에서 바로 Claude를 켜서 쓰는 법</strong>을 배웠다.
VSCode 안에서 쓰면 내가 지금 보고 있는 파일, 폴더 상태를 Claude가 바로 볼 수 있어서
따로 코드를 복사해서 붙여넣지 않아도 된다.</p>

<table>
  <thead>
    <tr>
      <th>방법</th>
      <th>언제 쓰나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>VSCode 밖에서 Claude 사용</td>
      <td>코드와 상관없는 일반적인 질문을 할 때</td>
    </tr>
    <tr>
      <td>VSCode 안에서 Claude 사용</td>
      <td>지금 보고 있는 프로젝트/파일에 대해 바로 물어볼 때</td>
    </tr>
  </tbody>
</table>

<h2 id="3-오늘-배운-것-ai에게-질문할-때-나눠서-쓰기">3. 오늘 배운 것: AI에게 질문할 때 나눠서 쓰기</h2>

<p>AI한테 그냥 두루뭉술하게 물어보면 원하는 답이 잘 안 나온다.
그래서 질문을 <strong>맥락 → 역할 → 제약 → 예시</strong>, 이렇게 네 부분으로 나눠서 쓰면 좋다는 걸 배웠다.</p>

<pre><code class="language-mermaid">flowchart TB
    A[맥락] --&gt; B[역할] --&gt; C[제약] --&gt; D[예시]
</code></pre>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>무엇을 적나</th>
      <th>언제 쓰나</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>맥락</td>
      <td>지금 상황이 어떤지</td>
      <td>왜 이 질문을 하는지 먼저 알려주고 싶을 때</td>
    </tr>
    <tr>
      <td>역할</td>
      <td>AI가 어떤 입장에서 답해야 하는지</td>
      <td>답변의 눈높이나 말투를 맞추고 싶을 때</td>
    </tr>
    <tr>
      <td>제약</td>
      <td>꼭 지켜야 할 조건</td>
      <td>답이 엉뚱한 방향으로 안 새게 하고 싶을 때</td>
    </tr>
    <tr>
      <td>예시</td>
      <td>원하는 답의 형태</td>
      <td>결과물이 어떤 모양이어야 하는지 보여주고 싶을 때</td>
    </tr>
  </tbody>
</table>

<p>이렇게 네 부분으로 나눠서 물어보면, AI가 내가 원하는 걸 훨씬 더 잘 이해하고 답해준다.</p>

<h2 id="오늘-정리하며-느낀-것">오늘 정리하며 느낀 것</h2>

<p>이번 주는 Git으로 코드를 저장하고 관리하는 법을 배웠고,
오늘은 그 위에 AI(Claude)를 도구로 잘 쓰는 법을 배웠다.
둘 다 결국 “내가 하는 작업을 기록해두고, 필요한 걸 명확하게 요청하는 것”이라는 공통점이 있는 것 같다.</p>]]></content><author><name>신아림</name></author><summary type="html"><![CDATA[오늘은 새로운 걸 두 가지 배웠다. Claude를 VSCode랑 같이 쓰는 법, 그리고 AI에게 물어볼 때 맥락/역할/제약/예시로 나눠서 써야 한다는 것이다. 전에 배운 Git 내용도 같이 정리해서, 지금까지 배운 걸 한 번에 모아본다.]]></summary></entry><entry><title type="html">push 하기 전에 pull 안 해서 겪은 git 충돌</title><link href="https://arim0317-dot.github.io/my-blog/git-pull-push-conflict/" rel="alternate" type="text/html" title="push 하기 전에 pull 안 해서 겪은 git 충돌" /><published>2026-08-31T00:00:00+09:00</published><updated>2026-08-31T00:00:00+09:00</updated><id>https://arim0317-dot.github.io/my-blog/git-pull-push-conflict</id><content type="html" xml:base="https://arim0317-dot.github.io/my-blog/git-pull-push-conflict/"><![CDATA[<p>오늘 블로그 글을 올리려고 <code class="language-plaintext highlighter-rouge">git push</code>를 했는데 처음으로 <strong>충돌(conflict)</strong>을 겪었다.
왜 났는지, 어떻게 풀었는지, 다음부터는 뭘 다르게 할지 정리해본다.</p>

<h2 id="1-무슨-일이-있었나">1. 무슨 일이 있었나</h2>

<p>로컬에서 글을 수정하고 바로 <code class="language-plaintext highlighter-rouge">git push</code>부터 했다.
그런데 그 사이에 원격 저장소(GitHub) 쪽에도 내가 모르는 변경사항이 먼저 올라가 있었고,
push가 거절되면서 결국 충돌이 났다.</p>

<h2 id="2-왜-충돌이-났을까">2. 왜 충돌이 났을까</h2>

<p>내 로컬 저장소와 원격 저장소가 이미 서로 다른 상태였는데,
<code class="language-plaintext highlighter-rouge">git pull</code>로 그 차이를 먼저 확인하지 않고 바로 <code class="language-plaintext highlighter-rouge">git push</code>를 해버린 게 원인이었다.</p>

<pre><code class="language-mermaid">flowchart LR
    subgraph 원격 GitHub
    R1((커밋 A)) --&gt; R2((커밋 B - 내가 모르는 변경))
    end
    subgraph 로컬
    L1((커밋 A)) --&gt; L2((커밋 C - 내가 만든 변경))
    end
    L2 -- "pull 안 하고 바로 push" --&gt; X[충돌!]
    R2 --&gt; X
</code></pre>

<p>같은 커밋 A에서 로컬과 원격이 각자 다른 방향으로 진행돼 있었으니,
git 입장에서는 “둘 중 뭘 남겨야 할지 모르겠다”고 알려준 셈이다.</p>

<h2 id="3-어떻게-해결했나">3. 어떻게 해결했나</h2>

<p>당황하지 않고 순서대로 진행했다.</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">git pull</code>로 원격의 최신 변경사항을 받아온다.</li>
  <li>충돌이 난 파일을 열면 아래처럼 표시가 생긴다. (실제 내용이 아니라 이해를 위한 예시 코드)</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD
내가 로컬에서 쓴 내용
=======
원격에 이미 올라가 있던 내용
&gt;&gt;&gt;&gt;&gt;&gt;&gt; abc1234 (원격 커밋)
</code></pre></div></div>

<ol>
  <li><code class="language-plaintext highlighter-rouge">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>, <code class="language-plaintext highlighter-rouge">=======</code>, <code class="language-plaintext highlighter-rouge">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code> 마커를 보면서 어느 쪽 내용을 남길지 직접 정하고, 필요 없는 부분과 마커 자체를 지운다.</li>
  <li><code class="language-plaintext highlighter-rouge">git add</code>로 수정한 파일을 다시 스테이징한다.</li>
  <li><code class="language-plaintext highlighter-rouge">git commit</code>으로 충돌을 해결했다는 커밋을 남긴다.</li>
  <li><code class="language-plaintext highlighter-rouge">git push</code>로 다시 올린다.</li>
</ol>

<pre><code class="language-mermaid">flowchart LR
    A[충돌 발생] --&gt; B[git pull]
    B --&gt; C["충돌 마커 확인 (&lt;&lt;&lt;&lt;&lt;&lt;&lt; / ======= / &gt;&gt;&gt;&gt;&gt;&gt;&gt;)"]
    C --&gt; D[남길 내용만 남기고 직접 수정]
    D --&gt; E[git add]
    E --&gt; F[git commit]
    F --&gt; G[git push]
</code></pre>

<h2 id="4-앞으로는-이렇게-하기로-함">4. 앞으로는 이렇게 하기로 함</h2>

<p><strong>push 하기 전에는 항상 pull 먼저 한다.</strong>
로컬과 원격이 안 맞을 가능성을 미리 없애는 게, 나중에 충돌 마커를 읽으면서 수정하는 것보다 훨씬 편하다는 걸 몸으로 배웠다.</p>

<h2 id="더-알아볼-것">더 알아볼 것</h2>

<ul>
  <li><strong>충돌 마커(<code class="language-plaintext highlighter-rouge">&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code>, <code class="language-plaintext highlighter-rouge">=======</code>, <code class="language-plaintext highlighter-rouge">&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code>) 제대로 읽는 법</strong> — 오늘은 뭐가 내 것이고 뭐가 원격 것인지 헷갈렸는데, 구조를 확실히 알아두면 다음엔 더 빨리 해결할 수 있을 것 같다.</li>
  <li><strong>merge와 rebase의 차이</strong> — 오늘은 merge 방식으로 풀었는데, rebase로 풀면 히스토리가 어떻게 달라지는지 다음에 알아볼 것.</li>
</ul>

<h2 id="참고-자료">참고 자료</h2>

<ul>
  <li><a href="https://git-scm.com/book/ko/v2/Git%EB%B8%8C%EB%9E%9C%EC%B9%98%EC%99%80-Merge-%EC%B6%A9%EB%8F%8C-%EC%9D%98-%EA%B8%B0%EB%B3%B8">Git 공식 문서 - 충돌 해결하기</a></li>
</ul>]]></content><author><name>신아림</name></author><summary type="html"><![CDATA[오늘 블로그 글을 올리려고 git push를 했는데 처음으로 충돌(conflict)을 겪었다. 왜 났는지, 어떻게 풀었는지, 다음부터는 뭘 다르게 할지 정리해본다.]]></summary></entry><entry><title type="html">Git Github</title><link href="https://arim0317-dot.github.io/my-blog/git-github/" rel="alternate" type="text/html" title="Git Github" /><published>2026-08-28T00:00:00+09:00</published><updated>2026-08-28T00:00:00+09:00</updated><id>https://arim0317-dot.github.io/my-blog/git-github</id><content type="html" xml:base="https://arim0317-dot.github.io/my-blog/git-github/"><![CDATA[<p>layout: post
title: “어제 배운 Git 정리”
date: 2026-08-28
—</p>

<h2 id="오늘-배운-것">오늘 배운 것</h2>

<p>Git은 <strong>저장 지점을 남기고 되돌릴 수 있게</strong> 해주는 도구다.</p>

<h3 id="커밋까지의-세-단계">커밋까지의 세 단계</h3>

<table>
  <thead>
    <tr>
      <th>단계</th>
      <th>명령</th>
      <th>하는 일</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td><code class="language-plaintext highlighter-rouge">git add</code></td>
      <td>커밋에 담을 것을 고른다</td>
    </tr>
    <tr>
      <td>2</td>
      <td><code class="language-plaintext highlighter-rouge">git commit</code></td>
      <td>고른 것을 한 덩어리로 기록한다</td>
    </tr>
    <tr>
      <td>3</td>
      <td><code class="language-plaintext highlighter-rouge">git push</code></td>
      <td>기록을 GitHub로 올린다</td>
    </tr>
  </tbody>
</table>

<p>가장 헷갈렸던 건 <code class="language-plaintext highlighter-rouge">add</code>와 <code class="language-plaintext highlighter-rouge">commit</code>이 왜 나뉘어 있는가였다.
파일을 두 개 고쳤는데 <strong>하나만 커밋하고 싶을 때</strong>가 있기 때문이었다.</p>

<h2 id="막혔던-것">막혔던 것</h2>

<p><code class="language-plaintext highlighter-rouge">git log</code>를 쳤더니 화면이 멈춘 것처럼 보였다.
알고 보니 기록을 보여주는 화면이 열린 것이었고, <code class="language-plaintext highlighter-rouge">q</code>를 누르면 빠져나온다.</p>

<h2 id="내일-볼-것">내일 볼 것</h2>

<ul>
  <li>마크다운으로 글 쓰기</li>
  <li>만든 것을 인터넷에 올리기</li>
</ul>]]></content><author><name>신아림</name></author><summary type="html"><![CDATA[layout: post title: “어제 배운 Git 정리” date: 2026-08-28 —]]></summary></entry></feed>